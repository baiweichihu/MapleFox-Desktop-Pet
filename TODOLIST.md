# TODOLIST — MapleFox 桌宠成品化

> 本文件记录 MapleFox 桌宠从「可运行的裁剪版」推进到「可分发成品」的所有已知待办。
> 优先级：P0（必须，影响基本可用）→ P1（重要）→ P2（增强）→ P3（长期/按需）。

---

## P0 — 基本可用性（完成这些才算是"能用的桌宠"）

### P0-1 MapleFox 多帧动画补充
- **现状**：`res/role/MapleFox/action/` 只有 `stand_0.png` 单帧，桌宠是静止的，不会走路/睡觉/拖拽动画。
- **待办**：为 MapleFox 补充动画帧，至少覆盖：
  - 待机 `stand`（可多帧做呼吸/眨眼）
  - 左右走 `left_walk` / `right_walk`
  - 睡觉 `fall_asleep` / `sleep`
  - 拖拽 `drag`、下落 `fall`、落地 `onfloor`、生气 `angry`
- **依赖**：需要 MapleFox 的动画帧素材（用户提供 或 AI 生成）。
- **完成后**：更新 `act_conf.json`，把各动作从 `stand` 指向真实帧。

### P0-2 打包为可分发成品
- **现状**：未打包，只能 `python run_DyberPet.py` 运行（依赖 conda 环境）。
- **待办**：用 PyInstaller 打包成 Windows 可执行文件（`.exe`），让用户双击即用、无需装 Python。
- **步骤**：
  1. 写 pyinstaller spec（含 `res/`、`data/`、翻译 `.qm` 等资源）
  2. 配置图标（MapleFox 专用 `.ico`）
  3. 打包并实机验证（单实例、存档、动画、设置均正常）
- **注意**：`tendo`（单实例）、`apscheduler`、PySide6 插件需正确打包。

---

## P1 — 品牌化与产品化

### P1-1 接入本地 LLM（基础对话）
- **背景**：桌宠需要 LLM 大脑做「基础对话」；选择本地部署（零成本、离线可用、隐私），详见 `scripts/llm-model-selection.md`。
- **定版模型**：Ollama + Qwen3-4B（Q4_K_M）——倾向 4B，定版前建议用同一 OC 人设 system prompt 实测 3B vs 4B（速度+质量）再定。
- **待办**：
  1. 安装/配置 Ollama，`ollama pull qwen3:4b`
  2. 桌宠侧接入 OpenAI 兼容 HTTP 接口（`base_url` + 模型名抽象成两处配置，便于日后切云端 API）
  3. ⚠️ Qwen3 关闭 thinking（`/no_think`），否则延迟翻倍
  4. 对话参数：`max_tokens` 64-128、streaming 开启、`temperature` ~0.8
  5. 对话历史保留最近 10-20 轮
  6. 接入 UI：对话入口（气泡/窗口）、发送/接收、流式显示
- **前置**：P0-1 动画完成（对话时桌宠需有说话/倾听状态）、P1-4 UI 重做方向确定。

### P1-2 桌面图标 / 窗口标题 / 启动画面品牌化
- **现状**：图标/标题沿用上游 DyberPet。
- **待办**：替换为 MapleFox 专属品牌：
  - 桌面/窗口图标（`res/icons/SystemPanel.png` 等）
  - 窗口标题（`setWindowTitle`）
  - 托盘图标、启动画面

### P1-3 右键菜单与界面文案最终精简
- **现状**：右键菜单已是 Dashboard/System/Memo/Exit。
- **待办**：确认是否还需调整（如去掉/改名某入口），以及各界面中文文案的完整性（含未翻译项，如 PetSize 已处理、其它可能遗漏）。

### P1-4 UI 重做（用户计划）
- **背景**：用户明确计划"之后重做所有 UI"。
- **待办**：这是大工程，需**单独拆分规划**。建议：
  1. 确认 UI 风格方向（简洁/拟物/扁平）、配色、字体
  2. 逐界面重做：系统设置、仪表盘（状态/背包/商店/动画）、右键菜单、对话框气泡、备忘录
  3. 沿用 qfluentwidgets 体系或引入新设计
- **注意**：UI 重做期间需保证功能逻辑不动，只换表现层。

---

## P2 — 增强与完善

### P2-1 死代码清理
- **现状**：部分模块删除后残留死代码（如 `custom_utils.py` 的 CharCard 系列、`DyberPet.py` 的 `act_menu`/`change_menu` 构建、`sig_settime_sche` 等）。
- **待办**：清理不再被引用的类/函数/导入，降低维护负担（建议在 UI 重做时一并处理，避免重复劳动）。

### P2-2 多语言完善
- **现状**：部分界面文案用 `self.tr()` 依赖翻译文件，新增功能（如 PetSize 已手动处理）可能未覆盖。
- **待办**：审查所有界面文案，补齐中文翻译（或统一手动判断方案）。

### P2-3 存档兼容性与健壮性
- **待办**：测试旧存档导入、损坏存档容错、快速存档/导出功能在裁剪后是否正常。

---

## P3 — 长期 / 按需

### P3-1 角色扩展预留
- 如果未来要出多个 MapleFox 变体（节日/季节限定），预留角色切换机制（当前已删，需评估是否重新引入）。

### P3-2 帮助/引导
- 为新用户补充首次启动引导、说明气泡，降低上手成本。

### P3-3 远程更新
- 若计划长期维护，可考虑重新引入「更新检查」（指向自有仓库/发布地址）。

---

## 变更记录

| 日期 | 内容 |
|:---|:---|
| 2026-08-28 | 创建 TODOLIST，梳理成品化剩余工作 |
| 2026-08-28 | 新增 P1-1 接入本地 LLM（Ollama + Qwen3-4B），详见 `scripts/llm-model-selection.md` |
| 2026-08-28 | 新增 `scripts/action-png-list.md`，列出 MapleFox 所需动作 PNG 清单 |
