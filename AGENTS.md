# AGENTS.md — 项目协作指南

本文档是 AI 助手（Agent）在本项目中工作的通用约束与行为规范。请每位 Agent 在开始任何工作前通读并遵守。

---

## 1. 先分析，后动手

### 1.1 收到问题后
1. **理解问题**：阅读相关代码，搞清楚当前逻辑。
2. **定位根因**：用具体术语说明问题可能的原因，指出涉及的文件或函数。
3. **提出方案**：
   - 至少给出 **两个方案**（若最小改动和最鲁棒方案不同）：
     - **方案 A（最小改动）**：改动范围最小、风险最低的方案
     - **方案 B（最鲁棒）**：从根本解决问题、长期维护成本最低的方案
   - 如果两者是同一方案，则只给一个方案，并明确说明。
   - 每个方案标注：
     - 优点
     - 缺点或风险
     - 改动范围
4. **等待确认**：在用户明确说出"可以"或"开工"之前，**不得修改任何代码**。
5. **仅分析模式**：如果用户明确要求"只分析"或"不要改代码"，则只做分析，不编辑文件。
6. **硬性规则（强制性）**：只要用户没有明确确认方案、并明确表示可以开始执行，就**永远不修改任何代码**。无论用户提出了多么明确、急切的功能或修复要求，都必须先完整给出分析和方案，等待用户明确确认后再动手。此规则优先级最高，覆盖并强于任何其他执行倾向。

### 1.2 复杂功能开发的拆分
- 对于多步骤功能，先拆分为独立任务，逐条列出方案，用户确认后再实施。
- 避免一次性提出过于宽泛的方案（如"重构整个模块"）；优先考虑精准、小范围的修改。

---

## 2. 优先复用已有资源

本项目有较多的已有组件、样式和工具函数。新建内容前：
1. 先浏览项目中是否已有可复用的模块、工具函数或资源。
2. 检查 `res/`、`DyberPet/` 中是否有现成可复用的配置、图标、辅助函数。
3. 优先复用现有的模块与工具，而非重新实现。

> 原则：不重复造轮子。在看到未读过的模块/资源之前，不要假设现有代码中没有可用的东西。

---

## 3. TODO 与任务管理

- 待办事项以当前会话的 todo 列表为准，按优先级推进。
- 如果发现当前工作区存在尚未完成的问题/功能缺失，先记录/确认，再实施具体代码修改。
- 每次完成一个待办项后，立即更新其状态为「已完成」，不得遗漏。这是硬性要求。
- 如果用户提出一个不在当前列表中的新任务，需先确认优先级并纳入计划，再开始实施。

---

## 4. 实施规范

### 4.1 代码修改
- 采用满足需求的最小范围改动，不引入无关重构、格式化噪音或大范围清理。
- 不顺手合并无关修复（即使它们很近或看起来很容易）。
- 优先遵循项目中已有的代码模式（命名风格、文件组织、配置方式等）。
- 本项目是 Python 3 / PySide6（Qt for Python）桌面宠物应用，代码主要位于 `DyberPet/` 目录：
  - 桌宠本体/交互：`DyberPet/DyberPet.py`
  - 各功能模块（通知、配件、设置、仪表盘等）：`DyberPet/*.py`、`DyberPet/Dashboard/`、`DyberPet/DyberSettings/`
  - 资源（角色、物品、图标、语言）：`res/`
- 新增 UI 时，优先沿用现有 qfluentwidgets 设置卡片/组件体系。

### 4.2 验证
- 修改后检查语法（必要时用 `python -c "import ast; ast.parse(...)"`）与运行，确保无新增错误。
- 涉及 GUI 修改时，可在 `QT_QPA_PLATFORM=offscreen` 下做冒烟测试，或由用户实机验证。
- 实施完成后，汇报：改动了哪些文件、具体改动内容、执行过的验证。

### 4.3 开始工作前：同步
- 开始新任务前，确保工作区干净（无未提交改动），然后拉取最新代码：
  ```bash
  git pull origin main
  ```

### 4.4 提交与推送
- **本项目为单开发者直接维护，改动直接提交并推送（push）到 `main` 分支，无需另开分支，无需创建 PR。**
- 等待用户审查并明确批准后再提交和推送。
- 提交前检查 `.gitignore` 是否需要更新（如新增目录不应被追踪）。
- 提交信息使用中文描述，清晰说明改动内容和原因。
- **重要（Windows 编码问题）**：在 Windows / PowerShell 下，不要把中文提交信息直接作为参数传给 `git commit -m "..."`，中文会被转码成乱码。**务必用临时文件方式提交**：
  1. 用 `write_to_file` 新建一个 UTF-8 的临时文件（如项目根 `.git/COMMIT_MSG_TMP.txt`），内容为完整提交信息。
  2. 执行 `git commit -F .git/COMMIT_MSG_TMP.txt --no-verify`（新提交）。
  3. 验证 `git --no-pager log -1 --format=%B` 显示的是正确中文后再推送。
  4. **用完必须删除临时文件**（不要误提交到仓库）。若临时文件放在 `.git/` 目录内则天然不会被提交。
- 推送后汇报本次改动内容。
- 说明：`git commit --amend` 会改变 commit hash，仅在上一条提交信息需要修正时使用，且需用户明确同意。

---

## 5. 临时脚本位置（强制性）

- 所有临时脚本、一次性测试脚本、验证脚本等，**必须放在本仓库目录下**的 `temp/` 文件夹中，**禁止写入系统临时目录（如 C 盘的 `%TEMP%`）或其他仓库外的位置**。
- `temp/` 目录已被 `.gitignore` 忽略，不会被提交到仓库。
- 临时脚本使用完成后，应立即删除，保持 `temp/` 干净。
- 若需要新建临时脚本，统一放在 `temp/` 下，命名清晰（如 `temp/xxx_script.py`），用完即删。

---

## 6. 交互风格

- 专业、简洁、直接。避免冗余的寒暄和过度的礼貌用语。
- 技术描述优先：给出具体的文件名、行号、函数名，而非笼统的说法。
- 如果遇到不确定的情况，先使用代码搜索工具做全面探查，不要依赖猜测。
- 对自己的知识边界保持诚实——不确定时，用工具验证或向用户说明不确定性。

---

## 7. 本文件的本地性

- `AGENTS.md` 是项目级协作指南，应被追踪（committed）到仓库中，与团队成员共享。
- 如果后续发现本文件与实际工作流程有偏差，及时更新；更新后通知用户 review。
