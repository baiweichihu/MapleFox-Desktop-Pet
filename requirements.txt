# MapleFox Desktop Pet 运行依赖
# 开发环境：conda env Dyber_pyside
#   pip install -r requirements.txt

PySide6>=6.5.0
PySide6-Fluent-Widgets>=1.5.0
requests>=2.31.0        # 本地 LLM 对话（OpenAI 兼容接口 / 流式输出）
tendo>=0.3.0            # 单实例
apscheduler>=3.10.0     # 提醒调度
pynput>=1.7.6           # 鼠标监听

# 可选：Font Awesome 图标（缺失时图标为空，不影响功能）
qtawesome>=1.3.0
